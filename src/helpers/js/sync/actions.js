import {
	REQUEST_APPLY,
	REQUEST_REVERT,
	APPLY,
	REVERT
} from '../../../common/actions/helpers';

export const receive = [
	REQUEST_APPLY,
	REQUEST_REVERT
];

export const send = [
	APPLY,
	REVERT
];
