import data from '../../../helpers/rgaa-3-2016';



/**
 *
 */
const initialState = data;

/**
 *
 */
export function helpers(state = initialState, {type, payload}) {
	switch (type) {
		default:
			return state;
	}
}
